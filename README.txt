SHEPHERDING ASSISTANT V2.24

Changes:
- Fixed mobile modal scrolling and prevented the page behind a publisher/call record from scrolling.
- Added a proper isolated scroll area so the bottom of long publisher records is reachable.
- Removed the accidental native calendar behavior from the publisher Last Shepherding Contact field; it is now a text date field accepting MM/DD/YYYY.
- Email fields now use email-specific iPhone keyboard/autocomplete settings.
- Dashboard Needs Attention publisher names are clickable and open that publisher's record.
- Added Schedule Shepherding Call inside each publisher record, with that publisher automatically selected.
- Added Previous / Next month controls to Calendar View.
- Empty calendar days now offer Schedule a return visit, with the date pre-filled and Return Visit selected.
- Added Return Visit as a call type.
- Kept the existing local-storage data key so existing group data is retained.
- Version: 2.24.
